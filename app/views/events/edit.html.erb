<h1>Редактирование события</h1>

<div class="row">
  <div class="col-md-6 mt-2">
    <%= render 'form', event: @event %>
  </div>
</div>

<div class="actions mt-3">
  <%= link_to 'Удалить событие', @event, method: :delete, data: { confirm: 'are u sure?' }, class: 'btn btn-danger' %>
</div>
